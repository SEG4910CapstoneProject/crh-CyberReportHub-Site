import { CreateTagDialogComponent } from './create-tag-dialog.component';

describe('CreateTagDialogComponent', () => {
  it('should be created', () => {
    expect(new CreateTagDialogComponent({} as any, {} as any)).toBeTruthy();
  });
});
